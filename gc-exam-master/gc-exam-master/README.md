# GC Exam Master (Florida GC – Contract Administration)

Client-side, weighted exam trainer with open-book references, spaced repetition, and a clean UI.

## Quick Start
```bash
npm i
npm run dev
```

## Build
```bash
npm run build
npm run preview
```

## Deploy (Vercel)
- Import repo on Vercel
- Framework: **Vite**
- Build: `npm run build`
- Output: `dist`
```

## Notes
- Tailwind via CDN in `index.html` (no config needed).
- Progress & spaced repetition stored in `localStorage`.
- No backend required; works on free hosting.
